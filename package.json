{
  "name": "bookstack-image-localizer",
  "version": "2.1.0",
  "description": "Find externally-hosted images across a BookStack wiki and re-host them locally, so they survive link rot",
  "type": "module",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "dev": "node --watch start.js",
    "build:app": "node build/build.mjs"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6"
  }
}
